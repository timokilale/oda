http://localhost:5173/restaurants/restaurant-2/menu/new here, as well as in ~/table/new, first the dialog pops even when i press Create n item/create n table, as well as add table/item card. These buttons should only add the configurable card below, then pressing the card pops up the dialog. Second issue, in the dialog, i can only type one word at a time, why? i have to manually click inside the text fields of the popup dialog each time i want to type. 

Second issue, the feedback for the new creation is not a snackbar like the one we have in successful order placements in the customer UI, why? AAAAALL feedback inside the system should use snackbar, nothing else. I see we are currently using this C:\Users\MSA WIN10 G\Desktop\odamobile\src\components\FlashStack.jsx, this is terrible UX.