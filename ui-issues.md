Here http://localhost:5173/restaurants/restaurant-2/orders, the order filters should be in a single row. The "filter by table name" should start, followed by a dropdown of the states of the orders - the completed, pending, etc. There should also be a last filter for "order by" for each column relevant/eligible.

Here http://localhost:5173/restaurants/restaurant-2/menu/new, first the category should be a dropdown as elsewhere, we don't allow user to specify their category. Also, the cardsare too big, let us do this, let us use popup dialogs for details. So when user is at http://localhost:5173/restaurants/restaurant-2/menu/new, they can add item cards, which initially will be empty with a plus sign at the center, where when the card is pressed, we pop up the dialog for the details. Also image addition should just be the generic "Choose file", remove the editing/crop container, not needed here. We do the same for table addition as well, for consistency across UI.

One more thing, why is this entire system not DOM? is it bootstrap, it refreshes the entire site in blinks for whatever new screen i go to. 
